import { P as ProviderOptions, O as OAuth2Tokens } from './types-DGbY_2Am.cjs';
export { a as OAuthProvider } from './types-DGbY_2Am.cjs';
export { g as generateState, p as parseState } from './state-DuyDp1Sf.cjs';
import 'zod';
import './helper-DPDj8Nix.cjs';

declare function createAuthorizationURL({ id, options, authorizationEndpoint, state, codeVerifier, scopes, claims, disablePkce, redirectURI, }: {
    id: string;
    options: ProviderOptions;
    redirectURI: string;
    authorizationEndpoint: string;
    state: string;
    codeVerifier?: string;
    scopes: string[];
    disablePkce?: boolean;
    claims?: string[];
}): Promise<URL>;

declare function validateAuthorizationCode({ code, codeVerifier, redirectURI, options, tokenEndpoint, }: {
    code: string;
    redirectURI: string;
    options: ProviderOptions;
    codeVerifier?: string;
    tokenEndpoint: string;
}): Promise<OAuth2Tokens>;

declare function generateCodeChallenge(codeVerifier: string): Promise<string>;
declare function getOAuth2Tokens(data: Record<string, any>): OAuth2Tokens;

export { OAuth2Tokens, ProviderOptions, createAuthorizationURL, generateCodeChallenge, getOAuth2Tokens, validateAuthorizationCode };
