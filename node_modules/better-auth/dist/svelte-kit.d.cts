import { B as BetterAuthOptions } from './auth-DQUo7qHe.cjs';
import 'zod';
import 'kysely';
import './types-DGbY_2Am.cjs';
import './helper-DPDj8Nix.cjs';
import 'better-call';
import './social.cjs';
import 'better-sqlite3';
import 'mysql2';

declare const toSvelteKitHandler: (auth: {
    handler: (request: Request) => any;
    options: BetterAuthOptions;
}) => (event: {
    request: Request;
}) => any;
declare const svelteKitHandler: ({ auth, event, resolve, }: {
    auth: {
        handler: (request: Request) => any;
        options: BetterAuthOptions;
    };
    event: {
        request: Request;
        url: URL;
    };
    resolve: (event: any) => any;
}) => Promise<any>;
declare function isAuthPath(url: string, options: BetterAuthOptions): boolean;

export { isAuthPath, svelteKitHandler, toSvelteKitHandler };
